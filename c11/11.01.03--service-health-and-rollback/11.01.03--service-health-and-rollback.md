## I didn't do the execercise in the section, just reading it


To delete the hello-world service:

```
$ docker service rm hello-world
hello-world

$ docker ps -a
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
```


